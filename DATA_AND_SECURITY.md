# 数据与安全边界

## 发布范围

本仓库是源码公开版，不提供原始监管附件、预构建知识库、预构建索引、开发评测数据或真实运行日志。任何由使用者自行构建的 `data/processed/`、`outputs/indexes/`、`knowledge_base/` 和 `evaluation/` 内容均属于本地派生产物，必须保持在 Git 忽略范围内。

## 数据责任

使用者自行获取数据，并负责确认数据的访问资格、隐私义务、处理条件、知识产权和再分发权。本项目不声明对任何第三方监管材料、网页附件、表格、文档或其派生内容拥有再分发授权。

## 凭证与部署

不要提交 `.env`、`.streamlit/`、`secrets.toml`、`credentials.json`、SSH 私钥、证书、数据库连接串或云服务凭证。部署时应由部署平台通过受控环境变量或密钥管理服务注入所需配置；本项目默认不需要 API Key。

## 发布前检查

在提交或发布前运行：

```bash
python scripts/check_public_release.py
```

该检查会拒绝已受 Git 跟踪的派生数据路径和常见密钥文件名，但不能代替对文本内容、Git 历史、第三方授权及 GitHub 平台侧日志、Release 附件、Actions 和协作者权限的人工审查。
